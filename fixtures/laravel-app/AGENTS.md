# Fixture app

This is a stand-in for `ticoncreserv/app`. Write code, identifiers, and comments in English.
Do not run `migrate:fresh`, `db:wipe`, or read `.env` files.
Workaround comments are normative and must not be removed without an explicit request.
